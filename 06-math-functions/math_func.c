#include <stdio.h>
#include <math.h>

int main() {

  // Most commun used

  double A = sqrt(9);
  double B = pow(2, 4);
  int C = round(3.14);
  int D = ceil(3.14);
  int E = floor(3.99);

  printf("%lf", A);

  return 0;
}
